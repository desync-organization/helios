# Promoted Adapter Pointer

No adapter is promoted. Member 2 owns atomic activation, and Member 1 must pass loader/rollback checks.
Only a reviewed `active.json` created after every gate in `adapter_manifest.py` passes belongs here.
